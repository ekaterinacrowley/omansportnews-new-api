{
  "name": "html-scss-js-codespace-template",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "watch:scss": "sass --watch src/scss/main.scss:public/styles.css --style=expanded",
    "watch:js": "esbuild --bundle src/js/main.js --outfile=public/main.js --watch",
    "watch:html": "node watch-html.js",
    "watch:html-build": "node watch-posthtml.cjs",
    "watch:images": "node -e 'require(\"chokidar\").watch(\"src/images\", {ignoreInitial:true}).on(\"all\", (event, path) => { console.log(new Date().toISOString(), event, path); require(\"child_process\").execSync(\"npm run copy:images\", {stdio: \"inherit\"}); })'",
    "watch:js-files": "node -e 'require(\"chokidar\").watch(\"src/js\", {ignoreInitial:true}).on(\"all\", (event, path) => { console.log(new Date().toISOString(), event, path); require(\"child_process\").execSync(\"npm run copy:js\", {stdio: \"inherit\"}); })'",
    "serve": "live-server public --port=5000 --watch=public",
    "copy:images": "mkdir -p public/images && cp -r src/images/* public/images/",
    "copy:components": "cp -r src/components public/components",
    "copy:js": "mkdir -p public/js && cp -r src/js/* public/js/",
    "build": "node scripts/inject-env-html.cjs && esbuild src/js/main.js --bundle --outfile=public/main.js && npx posthtml src/index.html -o public/index.html -c posthtml.config.cjs && cp -r src/scss public/scss && npm run copy:images && npm run copy:components && npm run copy:js",
    "dev": "npm run build && npm-run-all --parallel watch:scss watch:js watch:js-files watch:images watch:html-build start copy:images copy:components copy:js",
    "start": "node server.js --open",
    "proxy": "node watch-html.js"
  },
  "devDependencies": {
    "chokidar": "^3.5.3",
    "dotenv": "^16.0.0",
    "esbuild": "^0.18.0",
    "live-server": "^1.2.2",
    "npm-run-all": "^4.1.5",
    "posthtml": "^0.16.6",
    "posthtml-cli": "^0.10.0",
    "posthtml-include": "^1.7.4",
    "sass": "^1.75.0"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "express": "^5.1.0",
    "node-fetch": "^3.3.2",
    "swiper": "^11.2.10"
  },
  "description": "## Установка и запуск проекта",
  "main": "posthtml.config.js",
  "keywords": [],
  "author": "",
  "license": "ISC"
}
